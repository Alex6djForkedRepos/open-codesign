---
"@open-codesign/runtime": patch
"@open-codesign/desktop": patch
---

Allow interactive preview forms to run client-side submit handlers while blocking native form navigation with browser-enforced CSP. Keep standalone exports and connected development-server policies unchanged.
