---
"@open-codesign/desktop": patch
---

Include workspace CSV files in the existing bounded agent text-filesystem hydration. Seeded schedules and other local CSV references remain readable by the agent instead of appearing in the Files inventory but failing with "Path not found."
